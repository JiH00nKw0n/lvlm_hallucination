"""YAML loader: anchors + !ref + path resolution."""

import pytest
pytest.skip("TODO — verify !ref resolution against fixture YAML", allow_module_level=True)


def test_ref_resolves_subkey():
    pass
