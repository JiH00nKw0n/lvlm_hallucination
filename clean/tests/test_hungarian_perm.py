"""Hungarian perm correctness on a known synthetic case."""

import pytest
pytest.skip("TODO — random perm + identity correlation should round-trip", allow_module_level=True)


def test_perm_recovers_identity():
    pass
