"""save_stacked → load_stacked round-trip."""

import pytest
pytest.skip("TODO — write tiny array, read back, verify shape + meta", allow_module_level=True)


def test_save_load_roundtrip(tmp_path):
    pass
