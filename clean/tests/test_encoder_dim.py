"""Each encoder backend reports the dim claimed in its YAML."""

import pytest
pytest.skip("TODO — load each model YAML, confirm encoder.dim matches", allow_module_level=True)


def test_clip_b32_dim_512():
    pass
