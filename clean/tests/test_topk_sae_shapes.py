"""TopKSAE / TwoSidedTopKSAE forward-pass shape checks."""

import pytest
pytest.skip("TODO — instantiate models, push (B, dim) batch, assert recon shape", allow_module_level=True)


def test_topk_recon_shape():
    pass
