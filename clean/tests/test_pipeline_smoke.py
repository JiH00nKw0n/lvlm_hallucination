"""End-to-end smoke: a tiny CC3M run produces a valid output_root."""

import pytest
pytest.skip("TODO — point at fixture cache, run cc3m_downstream stage=train, "
            "assert output_root has ckpt + perm.npz", allow_module_level=True)


def test_cc3m_smoke():
    pass
