# === Core (always needed) ===
numpy>=2.0
scipy>=1.14
torch>=2.4
tqdm>=4.60
pydantic>=2.0
pyyaml>=6.0

# === Data ===
datasets>=4.0
pillow>=10.0
omegaconf>=2.3

# === ML / Transformers ===
transformers>=4.40
accelerate>=1.0
trl>=0.20
einops>=0.8

# === Visualization ===
matplotlib>=3.8
seaborn>=0.13

# === Utilities ===
packaging>=23.0
requests>=2.30
huggingface-hub>=0.20
safetensors>=0.4

# === Optional (for full project, not needed for synthetic experiments) ===
# peft>=0.15
# openai>=1.60
# tabulate>=0.9
# scikit-learn>=1.4
# wandb>=0.16
# opencv-python-headless>=4.8
# bitsandbytes>=0.43
# spacy>=3.7
# natsort>=8.4
# simple-parsing>=0.1
# flash-attn>=2.5
# aiohttp>=3.9
